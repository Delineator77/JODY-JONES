# Dev tooling only (screenshot harness + three.js vendoring source)
node_modules/
package-lock.json

# Transient render captures from the Gauntlet Loop critics
# (nested too — critic rounds capture into shots/round1/, shots/round2/, ...)
shots/*.png
shots/**/*.png
tools/webgl_test.html

# OS
.DS_Store

# Transient scratch scripts created by critic subagents
tools/_*.mjs

# Generated single-file build (rebuilt at release via tools/build-standalone.mjs)
dist/
